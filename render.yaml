services:
  - type: web
    name: forge-twin-ai-api
    env: docker
    healthCheckPath: /api/v1/health
    envVars:
      - key: ENVIRONMENT
        value: production
      - key: API_KEY
        sync: false
      - key: CORS_ORIGINS
        sync: false
